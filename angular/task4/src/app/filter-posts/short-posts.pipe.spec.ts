import { ShortPostsPipe } from './short-posts.pipe';

describe('ShortPostsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortPostsPipe();
    expect(pipe).toBeTruthy();
  });
});
